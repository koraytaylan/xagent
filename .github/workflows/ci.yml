name: CI

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

env:
  CARGO_TERM_COLOR: always

jobs:
  check-and-test:
    name: Check & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Install Mesa lavapipe (GPU software adapter)
        run: sudo apt-get update && sudo apt-get install -y mesa-vulkan-drivers vulkan-tools libvulkan1

      - name: Verify lavapipe installation
        run: vulkaninfo --summary || true

      - name: Rustfmt check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Cargo check
        run: cargo check --workspace

      - name: Cargo test
        run: cargo test -p xagent-sandbox
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
          XAGENT_REQUIRE_GPU: "1"

      - name: Benchmark (informational)
        if: github.event_name == 'push'
        continue-on-error: true
        run: cargo run --release -p xagent-sandbox -- --bench --bench-ticks 5000 --bench-agents 10 2>&1 | tee bench-output.txt

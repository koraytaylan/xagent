# Plan status

Roll-up board for `docs/plans/` — **one row per plan, no per-task detail**. Task-level status lives in each plan's own `STATUS.md` (linked in the last column). This board exists to answer "what is done, in flight, or blocked?" across all plans at a glance, and stays small as plan count grows.

**It is only useful if it is accurate.** Update the relevant row in the *same change* that moves a plan, and keep it in sync with that plan's `STATUS.md` (see [README → STATUS.md](README.md#statusmd)).

_Last updated: 2026-06-22, against `develop`._

| Plan | Title | Status | Tasks | Outcome | Detail |
|---|---|---|---|---|---|
| 0001 | Survival Signal Grounding | ✅ Complete | 10/10 | Deaths-per-food 4.02 → 2.23 (−45%); `lag100` confirmed default. | [status](0001-Survival-Signal-Grounding/STATUS.md) |
| 0002 | CPU/GPU Runtime Decoupling | ✅ Complete | 12/12 | Sim decoupled from redraw onto a GPU worker thread; shared-GPU render path rejected. | [status](0002-CPU-GPU-Runtime-Decoupling/STATUS.md) |
| 0003 | Simulation Throughput Ceiling | ✅ Complete | 8/8 | Fused single-submit dispatch landed & **bit-identical**; submit tax removed (10 000 batches → 417 submits). On-target A/B then **adjudicated the ceiling**: fusion engaged but tps ≈23 k unchanged, `global` pass only ≈3% → 0004 **rejected with evidence**; the real limiter is the fused **kernel/brain pass** (≥93%, agent-count-independent) → new plan. | [status](0003-Simulation-Throughput-Ceiling/STATUS.md) |
| 0004 | Approach Reward Shaping | ✅ Complete (prove-or-kill: **negative**) | 9/14 landed; 5 gated **correctly not opened** | `0001` shaping landed & mechanism-tested (PBRS `F=γΦ(s′)−Φ(s)` into `raw_gradient`; actor scale split 1/128→1/16), but the remeasure **falsified the unlock**: mirrored-steering alignment held at chance across 1200 shaped episodes. Encoder separability is adequate (between 0.0036 ≫ within 0.0002) → next suspect is the credit/temporal path, not the encoder. Gate closed `0002`/`0004`/`0005`. `0003` (unconditional) landed: foraging-primary fitness, significance guard, `num_islands→1`, governor within-life metric; the population-growing experiment-decoupling is **superseded** by the 2026-06-15 sweep (shared-world growth regresses foraging) and the shared-world architecture (no independent per-repeat seeds). | [status](0004-Approach-Reward-Shaping/STATUS.md) |
| 0005 | GPU Occupancy & Brain-Pass Latency | 🚧 In progress | all measured on target; occupancy→evolution win did not hold | `0001`+`0002` wiring merged & byte-identical/green: `--bench-agent-sweep`, `GpuKernel::has_subgroup()` + top-K-path log, `XAGENT_KERNEL_PASS_LIMIT` probe. **On target:** sweep confirms the GPU knee at N=200 (~10× throughput), but the fixed-seed validation showed raising the population **regresses** evolution (shared-world food competition; enlarging the world removes competition but yields no fitness gain — the limiter is learner strength) → **default kept at 10**. `0002` profile: cost is the dense NN passes (`learn_and_store` > `predict_and_act` > `encode`), **not** the bitonic top-K the plan assumed → `0003` **resolved as a measured negative** (reductions float-order-locked; cooperative-restructure lever deferred to a follow-up). | [status](0005-GPU-Occupancy-And-Brain-Latency/STATUS.md) |
| 0006 | Multi-Workgroup Brain Parallelism | ✅ Complete (60k **missed**; default **+96%**) | 10/10 | **TARGET MISSED: N=10 plateaus at 45,506 tps (75.8% of 60k).** The multi-workgroup-split hypothesis was **built and falsified** — `ParallelTiled` is slower than fused at N=10 (−6%) and N=200 (−28%): per-cycle dispatch overhead exceeds the parallelism gain (the 0003 scaffold already measured a 15.4% pure split tax). The win came from **same-dispatch tiling** of every dense loop + thread-0 chokepoint, all in the fused default: **23,230 → 45,506 tps (+96%)** (0002 +46%, action-tail 0004d +21%, learn_and_store 0004e +8.4%). Gap owner = the **10-workgroup occupancy ceiling at N=10** (irreducible matrix-vector FLOPs at ~25% GPU util; more workgroups needs the split, which the overhead blocks). Food = measured non-owner (in the 157,910 floor). Default stays `FusedSerial`; learning unchanged (probes at chance baseline). [`0006-60K-CLOSURE.md`](0006-Multi-Workgroup-Brain-Parallelism/0006-60K-CLOSURE.md). | [status](0006-Multi-Workgroup-Brain-Parallelism/STATUS.md) |
| 0007 | Learning Control Grounding | ✅ Complete | 12/12 | All 12 tasks landed via `implement-plan` (`c40dcb7`): live worker now applies per-agent heritable genes + records `movement_speed` provenance; recording format v2 (legacy-compatible) + nav telemetry slots (`P_NEAREST_FOOD_BEARING`, `P_IN_DANGER_BIOME`, turn-persistence) + `behavior_metric` table; slow learning curriculum (`movement_speed=8`, strides 2/5) + lowered `[4.0, 30.0]` speed range; authoritative turn-persistence EMA + sign-breaking klinotaxis; food-closure / danger-exit / anti-circle red-green probes. **Post-merge full-workspace gate caught a functional defect the per-task gates missed: `P_IN_DANGER_BIOME` was published inverted (made `danger_exit_probe` pass vacuously) — fixed (1.0=in danger) and telemetry tests rewritten to assert the flag against the agent's actual readback biome.** The 20-gen evolution comparison is an offline run, deferred via decision note. Gate green: fmt/clippy clean, 232 tests pass. | [status](0007-Learning-Control-Grounding/STATUS.md) |
| 0008 | Hubel-Wiesel Visual Encoder | ✅ Complete (mechanical; encoder default **HELD** on cost) | 14/14 | Biological early-visual-cortex pass shipped & **probe-proven**: dense luminance retina (curriculum 32×32) → DoG center-surround → 4×2×2 oriented Gabor simple cells → quadrature-energy + 4×4 MAX-pooled complex cells (128 features), with four heritable seeded global-bank genes; `visual_encoding_size` retired (closes #106); runtime genome authority reuses 0007's `patch_agent_configs`. **Probes PASS with margin** — a vertical bar excites the vertical-tuned cell **6.97×** the orthogonal (threshold 3×, isotropic control 1.0×); complex cells phase-invariant **~1.6e-7** (<10%) and position-tolerant **~7.8%** (<15%, 8px control breaks). **Throughput gate HELD:** at retina 32×32 / 128-feature bank cortex-ON retains only **~0.24%** of the 0006 fused baseline (≈81 vs ≈34,000 tps, ≈420× slower) — far under the ≥50% budget — so `visual_cortex_enabled` stays **default `false`** (ships complete behind the flag). SCOPE-declared **mechanical** success (filters correct), explicitly not a fitness gain. Cost-reduction follow-up (smaller retina, cheaper pool/bank, separable conv) re-runs the same gate; per-filter genome stays gated on a demonstrated evolutionary signal. [baseline/decision doc](0008-Hubel-Wiesel-Visual-Encoder/0008-VISUAL-CORTEX-BASELINE.md). | [status](0008-Hubel-Wiesel-Visual-Encoder/STATUS.md) |
| 0009 | Intent-Aware Fitness | ✅ Complete | 17/18 | Kills the speed exploit (raising `movement_speed` inflates foraging rate, cell coverage, and survival at once because every fitness axis is per-TIME and speed is the only gene that buys time). Three reinforcing layers + a percept: (A) super-linear locomotor energetics above baseline (`speed_cost_exponent`, keystone — single-peaked speed→fitness), (B) dwell-invariant path-length hazard (graded, never lethal — preserves the avoidance-decision data the user requires), (C) effort-rebased fitness (food-per-**energy** to defeat camping, cells-per-distance capped by coverage), (D) dedicated danger percept + symmetric potential-based avoidance shaping + sensed-then-turned intent metric. All behind flags, default byte-identical; graduated on a headless **speed-decoupling** gate (correlation `movement_speed`↔fitness → ≈0 / single-peaked, no `ticks_alive` collapse, danger data retained). `default-flip-gate` gated pending measured decoupling confirmation. | [status](0009-Intent-Aware-Fitness/STATUS.md) |
| 0010 | Intent-Aware Fitness Hardening | ✅ Complete | 21/21 | Makes 0009's flags safe to graduate (flips no default). All 21 tasks landed via `implement-plan` (`b291dafb`); flags stay default-off / byte-identical; fmt+clippy clean, 217 tests pass. (0001) brain metabolic drain added to `P_ENERGY_SPENT`, effort calibration re-derived from the **recorded-generation replay** with a production-scale assertion; (0002) speed-decoupling gate rewritten (strongly-positive baseline + strict improvement, uncapped viability, seeded **paired** A/B, GPU-free unit tests, self-describing decision doc, real avoidance floor); (0003) avoidance-intent **sign inversion** + fused stale-telemetry ordering fixed, danger ring-scan flag-gated, `atan2(0,0)` guarded; (0004) the three acceptance-named tests now assert their titular property + `WC_*`/`CFG_*` parity + serde defaults + migration order + stale-layout/magic-number fixes. **A fused/split divergence (surfaced by the brain-drain accounting) failed the first parallel run; re-run serially to convergence.** `gate-includes-avoidance-floor` folded into `harden-gate-predicate`. Running 0009's `default-flip-gate` to a decision is the follow-on. | [status](0010-Intent-Aware-Fitness-Hardening/STATUS.md) |
| 0011 | Source Planning-Reference Scrub | ✅ Complete | 5/5 | Pays the CONTRIBUTING "source agnostic of planning" debt the Grok 4.3 / Gemini 3.1 Pro reviews flagged, on the files Plan 0010 does **not** touch (disjoint → fully parallel). Ratchets `contributing_guard.rs::PLANNING_REFERENCE_BASELINE` down across 13 files / 44 reference-lines (`brain_passes.wgsl` 20 the heaviest), replacing each plan tag with its technical rationale and removing the uncaught `Layer …`/`speed-decoupling`/bare `0008`/`0006` vocabulary; comment/string hygiene only, no behavioral change. | [status](0011-Source-Planning-Reference-Scrub/STATUS.md) |
| 0012 | Homeostatic-Only Learning Restoration | ✅ Complete | 7/7 | Removes both within-life PBRS shaping terms (Plan 0004 approach + Plan 0009 avoidance) from `raw_gradient`, restoring the README's homeostatic-only contract by construction (corroborated by `2026-06-19-claude-opus-48.md` M1). Deletes the orphaned `APPROACH_SHAPING_GAIN`, renames the still-live `SHAPING_RADIUS`→`FOOD_SENSE_RADIUS` (food-detect), reserves the `P_PREV_*` slots, rewrites the shaping-verifying tests, and adds a homeostatic-only regression gate. The removed approach term was already falsified as ineffective, so the default signal is simplified without an expected behavior change. | [status](0012-Homeostatic-Only-Learning-Restoration/STATUS.md) |
| 0013 | Innate Survival Instincts | ✅ Complete | 6/7 | Seeded instinct priors (danger→avoidance, food→approach) initialized in pattern memory at brain birth, heritable via evolved strength genes, gated behind `innate_instincts_enabled` (default off), validated via a seeded-paired headless A/B (survive/steer/eat). Motivated by the `2026-06-19` credit-path diagnosis (steering at chance because the turn channel can't be credited) — instincts seed the basics the credit path can't yet teach. Pass/fail recorded in a decision doc; flag stays off until the gate passes. `author-ab-gate-decision-doc` gated (not run). | [status](0013-Innate-Survival-Instincts/STATUS.md) |
| 0014 | Post-0010 Review Hardening | 📋 Planned | 0/6 | Fixes the three 2026-06-19 due-diligence findings the reward-model re-think doesn't cover: (0001) remove the `ticks_alive` factor from both effort-fitness axes so a camper is no longer the fittest archetype (Camper 0.8646 > Competent 0.3209) + add the missing `competent > camper`/duration-invariance guards (flag stays default-off); (0002) install Mesa lavapipe in CI + a required-adapter gate so the ~63 GPU-gated tests actually run (CI was silently skipping the GPU core); (0003) fix the 8× terminal-death TD actor-scale divergence (`phase_death.wgsl` `TD_VECTOR_SCALE`→`ACTOR_VECTOR_SCALE`) + a death-crossing parity test. | [status](0014-Post-0010-Review-Hardening/STATUS.md) |

**Status legend:** 📋 Planned (authored, not started) · 🚧 In progress (some tasks merged) · ✅ Complete (all tasks merged to `develop`) · ⛔ Blocked (waiting on a gate or upstream task) · 🗄️ Superseded (replaced or abandoned).
